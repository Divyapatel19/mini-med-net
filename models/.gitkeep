This directory stores trained model weights.

Files in this directory are git-ignored (large binary files).
The demo weights file (mini_med_net_demo.pth) can be generated by running:

    python scripts/generate_demo_model.py

The best training checkpoint (mini_med_net_best.pth) is produced by:

    python src/training/train.py --config config/training_config.yaml
